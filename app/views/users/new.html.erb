<h1>New user</h1>

<% form_for(@user) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :user_name %><br />
    <%= f.text_field :user_name %>
  </p>
  <p>
    <%= f.submit 'Create', :class => 'text-center' %>
  </p>
<% end %>

<%= link_to 'Back', users_path %>
